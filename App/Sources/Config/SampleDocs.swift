import Foundation

/// Ссылки на реальные учебные документы, которые используются в демо-режиме
/// и в UI-тестах. Держим их в одном месте, чтобы правка ссылки была одна.
enum SampleDocs {
    /// Документ ученицы Ксюши: расписание занятий и пройденные темы по русскому,
    /// математике и английскому. Доступ «по ссылке», поэтому документ читается
    /// внутри приложения без входа в Google.
    static let ksyusha = URL(
        string: "https://docs.google.com/document/d/1GJCIgRcxw_RF4ku98-7-uQwNv4I6Y_Gn8SSJQ9aYCN0/edit?tab=t.0"
    )!
}
